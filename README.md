# Phiên Chợ Vùng Cao - Class Market Website

This is a premium, responsive website for your class market fair.

## 📁 Project Structure

- `index.html`: The main page content.
- `style.css`: Styling and animations.
- `main.js`: Logic for animations and menu.
- `public/images/`: **Place your images here!**

## 🖼️ How to Add Your Images

You mentioned you have prepared images. Please rename them and place them in the `public/images/` folder as follows:

1.  **Main Background**: Rename to `hero-bg.jpg`
2.  **Section 1 Image (Hồn Núi)**: Rename to `hon-nui.jpg`
3.  **Section 2 Image (Cảnh Mây)**: Rename to `canh-may.jpg`

*Note: If you don't rename them, you can open `index.html` and change the `src` attribute of the `<img>` tags to match your filenames.*

## 🚀 How to Run

1.  Open a terminal in this folder.
2.  Run `npm install` (if you haven't already).
3.  Run `npm run dev` to start the local server.
4.  Open the link shown (usually `http://localhost:5173`) in your browser.

## 🎨 Customization

- To change colors, edit the `:root` variables at the top of `style.css`.
- To change text, edit `index.html`.
